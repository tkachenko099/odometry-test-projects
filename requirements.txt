# Analysis dashboard & experiment-runner stack (Section 18 / 21 of the plan).
# Versions pinned for reproducibility; installed into /opt/venv inside the image.

# --- Dashboard (Plotly / Dash) ---
dash==2.18.2
dash-bootstrap-components==1.6.0
plotly==5.24.1

# --- Numerics / data handling ---
numpy==1.26.4
pandas==2.2.3
scipy==1.14.1

# --- Geodesy / navigation helpers ---
pymap3d==3.1.0
transforms3d==0.4.2
utm==0.7.0

# --- Experiment orchestration & IO ---
pyyaml==6.0.2
matplotlib==3.9.2
kaleido==0.2.1          # static PNG export for trajectory.png / *_error.png
tqdm==4.66.5

# --- Quality / tests for the Python side ---
pytest==8.3.3
pytest-cov==5.0.0
ruff==0.6.9
